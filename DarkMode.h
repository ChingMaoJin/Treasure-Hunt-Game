#ifdef DARK_MODE
#include "Game.h"
void UpdateGame(Position *P, Array *gameArray);
int HasLantern(Position *P);
int CalcManhattanDistance(int PlayerRow, int PlayerCol, int ObjectRow, int ObjectCol);
int HasLantern(Position *P);
#endif